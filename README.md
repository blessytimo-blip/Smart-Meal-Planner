# Smart-Meal-Planner
Console-based Python application that generates meal recipes using a Large Language Model (LLM) based on user inputs. The generated recipes are stored in a SQL database for persistence. No predefined recipe dataset is used.
